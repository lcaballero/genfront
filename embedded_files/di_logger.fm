package {{ .GOPACKAGE }}

import (
	seelog "github.schq.secious.com/Logrhythm/Godeps/_workspace/src/github.com/cihub/seelog"
)

var log seelog.LoggerInterface = seelog.Disabled

func DisableLog() {
	log = seelog.Disabled
}

func UseLogger(logger seelog.LoggerInterface) {
	log = logger
}

func FlushLog() {
	log.Flush()
}

